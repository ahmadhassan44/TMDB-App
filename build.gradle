buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.0'
        // Add other dependencies as needed
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}